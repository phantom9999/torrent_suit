sudo: required
dist: trusty
language: cpp

compiler:
    - gcc

cache:
    pip: true
    ccache: true
    directories:
     - /home/travis/.conan/
before_install:
    - sudo apt-get update -y

install:
    - sudo apt-get install cmake3; alias cmake='cmake3'
    - sudo pip install conan
    - conan remote add bincrafters https://api.bintray.com/conan/bincrafters/public-conan
    - sudo conan install . --build=missing

script:


after_success:
    -



